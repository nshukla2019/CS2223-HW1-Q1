public class main {
    // stackulator on pg 129 of textbook
    public static void main(String[] args) {

        // create an instance of the class Stackulator
        Stackulator myStackulator = new Stackulator();

        // run Stackulator method with the instance created above
        myStackulator.Stackulator();
    }
}
